---

**weewx-evo** — a rebuild of the WeeWX core.
The one rule: *an existing WeeWX database stays readable and writable — by WeeWX itself.*

This wiki is maintained by hand; [Index](Index) and [API-Index](API-Index) are generated by `tools/docsindex.py`.
To check whether a page is behind the code: `python tools/docsindex.py --check`
